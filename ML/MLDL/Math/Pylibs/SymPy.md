Библиотека для символьных вычислений на Python.

## Создание символов
Переменные необходимо явно объявить как символьные переменные, создав экземпляры класса `Symbol`
```python
x = symbols("x")
a, b, c = symbols("a b c")
```



